/**
 * 회고 도메인 — Retrospective. (BE-B 담당)
 *
 * <p>선택 기간의 DailyChatSession 전체 대화 원문 + persona.md 로 생성(동기, 201).
 * Diary 는 회고 생성의 직접 입력이 아니다. Export 서버 API 는 두지 않는다(FE 처리).
 */
package com.girinlog.retrospective;
