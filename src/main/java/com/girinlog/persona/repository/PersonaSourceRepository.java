package com.girinlog.persona.repository;

import com.girinlog.persona.domain.PersonaSource;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PersonaSourceRepository extends JpaRepository<PersonaSource, Long> {
}
